CREATE DATABASE IF NOT EXISTS greenshift_reports;
USE greenshift_reports;

CREATE TABLE IF NOT EXISTS high_accuracy_reports (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NULL,
    electricity_data TEXT,
    water_data TEXT,
    fuel_data TEXT,
    building_data TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);